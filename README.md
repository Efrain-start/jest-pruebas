# Pruebas unitarias con Jest

Este proyecto es una práctica de pruebas unitarias utilizando **Jest** y **JavaScript puro**.

## Funciones probadas
- `sumArray(numbers)`
- `countWords(text)`
- `findMax(numbers)`
- `isDivisible(num, divisor)`

## Cómo ejecutar los tests

```bash
npm install
npm test
